import 'package:flutter/material.dart';

class FormularioView2 extends StatelessWidget {
  const FormularioView2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('formulario2'),
      ),
      body: Center(
        child: const Text('Vista formulario2'),
      ),
    );
  }
}
