import 'package:flutter/material.dart';

class Formulario2View extends StatelessWidget {
  const Formulario2View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('formulario2'),
      ),
      body: const Center(
        child: Text('Vista formulario2'),
      ),
    );
  }
}
